# URL of the FastAPI backend (no trailing slash).
# Local dev default points at the FastAPI server you run with `uvicorn src.api:app`.
# On Vercel, set API_URL to the Fly deployment (e.g. https://causal-engine.fly.dev)
# in Project Settings -> Environment Variables (Production, Preview, Development).
API_URL=http://localhost:8000
